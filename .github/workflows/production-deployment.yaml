name: Production Deployment

on:
  push:
    branches:
      - production

permissions:
  id-token: write
  contents: read

jobs:
  staging:
    uses: ./.github/workflows/deployment.yaml
    with:
      environment: prod
    secrets: inherit
