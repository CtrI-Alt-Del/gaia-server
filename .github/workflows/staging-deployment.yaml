name: Staging Deployment

# fiz bagulho
on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  staging:
    uses: ./.github/workflows/deployment.yaml
    with:
      environment: dev
    secrets: inherit
