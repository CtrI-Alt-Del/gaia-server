name: Staging Deployment

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  staging:
    uses: ./.github/workflows/deployment.yaml
    with:
      environment: dev
    secrets: inherit
